<h1>Burgers!</h1>

<table>
  <tr>
    <th>Burgers not devoured!</th>
    <th>Burgers devoured!</th>
  </tr>
  <tr>
    <td>
      {{#each burger}}
      {{#if devoured}}
      {{> burger/burger-block devour=false}}
      {{/if}}
      {{/each}}
    </td>
    <td>
      {{#each burger}}
      {{#unless devoured}}
      {{> burger/burger-block devour=true}}
      {{/unless}}
      {{/each}}
    </td>
  </tr>
</table>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bn">Burger Name:</label>
    <input type="text" id="bn" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Devoured?</label><br>
    <input type="radio" name="devoured" value="0" checked> Devoured!<br>
    <input type="radio" name="devoured" value="1"> Eat me!
  </div>

  <button type="submit">Add Burger</button>
</form>